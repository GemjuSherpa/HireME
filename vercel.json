{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "git": { "deploymentEnabled": false },
  "regions": ["syd1"],
  "buildCommand": "prisma generate && next build",
  "crons": [{ "path": "/api/cron/invitations", "schedule": "0 1 * * *" }]
}
